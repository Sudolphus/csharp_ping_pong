using System.Collections.Generic;

namespace PingPong.Play
{
  public class PingPong
  {
    public static string PlayPingPong(int maxNumber)
    {
      List<string> outputList = new List<string>(0);
      for (int i = 1; i <= maxNumber; i++)
      {
        
      }
    }
  }
}